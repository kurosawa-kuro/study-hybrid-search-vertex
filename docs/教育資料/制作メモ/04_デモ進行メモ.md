# デモ進行メモ (Phase 4 / Vertex 編)

詳細版は `../03_デモシナリオ.md` を参照。ここでは撮影順だけを残す。

---

## 進行

1. `app/src/app/ports/encoder_client.py` / `reranker_client.py` を開いて新 Port を確認
2. `app/src/app/adapters/vertex_prediction.py` で adapter の薄さを確認
3. `app/src/app/entrypoints/api.py` の lifespan で Vertex クライアント DI を確認
4. `app/src/app/services/ranking.py` が **変わっていない** ことを確認
5. `infra/modules/vertex/main.tf` で Feature Group + 2 Endpoints + Cloud Function + Eventarc x 2 を確認
6. `jobs/src/training/entrypoints/{encoder,reranker}_server.py` を確認
7. `pipelines/src/pipelines/property_search/{embed,train}_pipeline.py` を確認
8. `functions/pipeline_trigger/main.py` で Pipeline submit 経路を確認
9. `uv run python -m scripts.setup.setup_encoder_endpoint` で spec 出力
10. `uv run python -m scripts.setup.create_schedule --target all` で 3 経路 CT のうち (c) 経路を確認
11. `scripts/ops/promote.py` と 9 SA (`infra/modules/iam/main.tf`) を確認
12. 締め — 「境界を守って差し替える」の再確認

---

## 画面で確認するもの

### コード (変わった / 変わっていない)
- lifespan の Vertex クライアント DI
- vertex_prediction の薄さ
- `ranking.run_search` の骨格 (RRF -> rerank -> top-K)
- Port に `google.cloud` import が **入っていない** こと
- `/events/retrain` が **存在しない** こと

### Vertex スペック (JSON 出力)
- `setup_encoder_endpoint` の `serving_container_image_uri` / `artifact_uri` / `service_account`
- `create_schedule` の embed 日次 / train 日曜 cron
- `setup_model_monitoring` の `feature_names` (property-side 7 列)

### IaC
- `infra/modules/vertex/main.tf` の 3 ブロック (Feature Group / Endpoints / Cloud Function+Eventarc)
- 9 SA の一覧

---

## 一言メモ

- 実 GCP 投入は未実施前提、spec / コード walk 中心
- デモの強調軸は「差分工事」「境界維持」
- reranker Endpoint deploy は train パイプラインが自動、encoder は手動スクリプト 2 本
- Doppler / Matching Engine / VECTOR INDEX IaC は触れる程度に留める (scope 外)
