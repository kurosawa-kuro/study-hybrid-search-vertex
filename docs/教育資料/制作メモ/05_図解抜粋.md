# 図解抜粋 (Phase 4 / Vertex 編)

詳細版は `../05_図解.md`。ここでは一枚絵の要素だけを箇条書きで残す。

---

## Phase 3 -> Phase 4 差分

- `search-api lifespan load` -> `Vertex Endpoint x 2` (encoder / reranker)
- `Cloud Run Jobs embedding-job / training-job` -> `Vertex AI Pipelines x 2` (embed / train)
- `GCS + training_runs` -> `Vertex AI Model Registry` (+ training_runs dual)
- `Scheduled Query skew` -> `Vertex Model Monitoring v2` (SQL は backup)
- `Eventarc -> Cloud Run Job` -> `Eventarc -> Cloud Function pipeline-trigger`
- `CR Service 4Gi` -> `CR Service 2Gi`
- `5 SA + WIF` -> `9 SA + WIF`

---

## 全体像 (Phase 4)

- `Dataform -> feature_mart -> Vertex Feature Group`
- `embed_pipeline -> encoder Endpoint (Batch Prediction) -> property_embeddings`
- `search-api -> encoder Endpoint / Meili / VECTOR_SEARCH -> RRF -> reranker Endpoint`
- `train_pipeline -> Vizier -> train_reranker -> evaluate -> register_reranker -> Endpoint canary`
- `Monitoring / Scheduler / PipelineJobSchedule -> Cloud Function pipeline-trigger -> PipelineJob.submit()`

---

## Port/Adapter 増設

- ports: `encoder_client.py` / `reranker_client.py` (Protocol のみ)
- adapters: `vertex_prediction.py` (`VertexEndpointEncoder` / `VertexEndpointReranker`)
- 削除: `app/services/model_store.py` / `app/adapters/model_store.py` / `app/ports/model_store.py` / `app/adapters/training_job.py` / `app/ports/training_job_runner.py`

---

## 6 ファイル parity

- (1) Dataform SQLX / (2) feature_engineering.py / (3) infra/data ranking_log RECORD / (4) monitoring SQL / (5) schema.py / **(6) infra/modules/vertex Feature Group**
- query-time 3 列 (me5_score / lexical_rank / semantic_rank) は Feature Group と monitoring SQL から除外

---

## CT 3 経路 (共通 sink: Cloud Function pipeline-trigger)

- (a) Cloud Scheduler -> `/jobs/check-retrain` -> `retrain-trigger` topic
- (b) Vertex Model Monitoring v2 -> `model-monitoring-alerts` topic (enable_tuning=True)
- (c) PipelineJobSchedule cron (embed 日次 / train 日曜)

---

## 9 SA 分離

- `sa-api` (+aiplatform.user)
- `sa-pipeline` (job_train rename)
- `sa-endpoint-encoder` / `sa-endpoint-reranker` (GCS reader のみ)
- `sa-pipeline-trigger` (Cloud Function 専用、`sa-pipeline` を actAs)
- `sa-job-embed` / `sa-dataform` / `sa-scheduler` / `sa-github-deployer` (継続)
